{% extends 'base.html.twig' %}

{% block title %}Borrowing{% endblock %}

{% block body %}
    <h1>Borrowing</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ borrowing.id }}</td>
            </tr>
            <tr>
                <th>Date_borrowed</th>
                <td>{{ borrowing.dateBorrowed ? borrowing.dateBorrowed|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Datereturned</th>
                <td>{{ borrowing.datereturned ? borrowing.datereturned|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_borrowing_index') }}">back to list</a>

    <a href="{{ path('app_borrowing_edit', {'id': borrowing.id}) }}">edit</a>

    {{ include('borrowing/_delete_form.html.twig') }}
{% endblock %}
